import styled from 'styled-components';

const ListStatistics = styled.ul`
  list-style: none;
`;
const ItemStatistics = styled.li`
  font-size: 25px;
  padding-bottom: 5px;
`;
const Green = styled.span`
  color: green;
`;
const Orange = styled.span`
  color: orange;
`;
const Red = styled.span`
  color: red;
`;

export { ListStatistics, ItemStatistics, Green, Orange, Red };
